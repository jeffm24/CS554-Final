const Profile = React.createClass({
    render() {
        return (
            <div>
                <p>Profile</p>
            </div>
        );
    }
});

ReactDOM.render(<Profile/>, document.getElementById('profile-component'));
