const data = require('../data');

describe('User data tests', function() {
    it('should delete a SINGLE blob on /blob/<id> DELETE', (done) => {
        done();
    });
});

describe('Ticker data tests', function() {
    it('should delete a SINGLE blob on /blob/<id> DELETE', (done) => {
        done();
    });
});

describe('Historical data tests', function() {
    it('should delete a SINGLE blob on /blob/<id> DELETE', (done) => {
        done();
    });
});